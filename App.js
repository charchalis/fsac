/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 *
 * @format
 * @flow strict-local
 */

import React from 'react';

import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';


const App = () => {
  
  

  return (
      <Text style={{flex: 1, backgroundColor: "#091212"}}>words and stuff</Text>
  );
};

const styles = StyleSheet.create({
  
});

export default App;
